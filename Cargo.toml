[package]
name = "herdr-nvim"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
crossterm = "0.28"

[dev-dependencies]
regex = "1"

[profile.release]
lto = true
strip = true
